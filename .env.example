# ============================================
# SubTranslate — Environment Variables
# ============================================
# Used by docker-compose.yml for all services.
# Copy to .env (production) or .env.local (local dev) and fill values.
#
# Coolify: vars with ${VAR:?} are auto-detected as required in UI.
# Coolify: SERVICE_PASSWORD_REDIS is auto-generated (magic var).

# --- Required (deployment fails without these) ---

# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
SUPABASE_ANON_KEY=your_anon_key_here

# CORS (your frontend domain — must match exactly)
CORS_ORIGINS=https://yourdomain.com

# Frontend build args (baked into JS bundle at build time)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# --- Optional ---

# Cloudflare R2 Storage
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_ENDPOINT=
R2_BUCKET_NAME=subtranslate
R2_CDN_DOMAIN=

# AI Translation Keys (system-level defaults)
OPENAI_API_KEY=
DEEPL_API_KEY=
GEMINI_API_KEY=

# Debug mode (false = Swagger UI disabled)
DEBUG=false

# --- Coolify Magic Variables (auto-generated, do NOT set manually) ---
# SERVICE_PASSWORD_REDIS  — secure password for internal Redis
# SERVICE_URL_BACKEND     — https://your-backend-domain.com
# SERVICE_FQDN_BACKEND    — your-backend-domain.com
# SERVICE_URL_FRONTEND    — https://your-frontend-domain.com
# SERVICE_FQDN_FRONTEND   — your-frontend-domain.com

# --- Local Development Only ---
# For local docker-compose, set SERVICE_PASSWORD_REDIS manually:
# SERVICE_PASSWORD_REDIS=localdev123
